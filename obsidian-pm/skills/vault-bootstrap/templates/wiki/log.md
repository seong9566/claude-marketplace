# 작업 로그

시간순 기록. 의미 있는 작업마다 아래 양식으로 append한다. 과거 기록은 소급 수정하지 않는다.

```markdown
## [YYYY-MM-DD] type | 제목
- 요약: ...
- 변경 파일: ...
- 후속 작업: ...
```

`type`: `setup` / `ingest` / `query` / `lint` / `output` / `maintenance`.
